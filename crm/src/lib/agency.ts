/**
 * Agency identity used on generated ACORD forms (producer block).
 *
 * The values live at the repo root in `shared/agency.ts` so that this app and
 * the marketing site cannot drift apart — the field names here already matched
 * that module's, so this file is now just the `crm`-side name for it.
 */
export { AGENCY } from "../../../shared/agency";
