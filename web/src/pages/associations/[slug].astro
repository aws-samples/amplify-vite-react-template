---
import { AssociationLeadForm } from "../../components/AssociationLeadForm";
import propertiesData from "../../data/properties.json";

interface Property {
  id: number;
  name: string;
  slug: string;
  address: string;
  city: string;
  state: string;
  zip: string;
}

const properties = propertiesData as Property[];

export function getStaticPaths() {
  const props = (propertiesData as Property[]).map((p) => ({
    params: { slug: p.slug },
    props: { property: p },
  }));
  return props;
}

const { property } = Astro.props;

const fullAddress = [property.address, property.city, property.state, property.zip]
  .filter(Boolean)
  .join(", ");
import Analytics from "../../components/Analytics.astro";
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="apple-touch-icon" href="/favicon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#1a365d" />

    <!-- Private association links — not for organic indexing -->
    <meta name="robots" content="noindex, nofollow" />
    <meta name="googlebot" content="noindex, nofollow" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />

    <title>HO-6 Condo Insurance for {property.name} — ProtectMyHOA</title>
    <meta name="description" content={`Get an HO-6 condo insurance quote for your unit at ${property.name}${fullAddress ? ` (${fullAddress})` : ""}. Coordinated with your association's master policy. Free quote — no obligation.`} />

    <Analytics />
  </head>
  <body style="margin:0; font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif; -webkit-font-smoothing: antialiased;">
    <AssociationLeadForm client:load property={property} />
  </body>
</html>
<!-- Generated from Buildium sync. {properties.length} associations indexed. -->
